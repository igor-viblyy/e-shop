=simple_form_for [:admin, @category], html: { role: 'form' } do |f|
  .form-group
    =f.input :name, label: false, placeholder: 'Add name Category', input_html: { class: 'form-control' }
  -#.form-group
  -#  = f.select :parent_id, nested_set_options(Category, @category) { |i| "#{'-' * i.level} #{i.name}" }
  -#  = select_tag "parent_id", options_for_select( nested_set_options(Category) { |i| "#{'-' * i.level} #{i.name}" } )
  -#  -@category.child.each do |sub|
  -#    =f.simple_fields_for :child, sub do |child|
  -#      =child.input :name, label: false, placeholder: 'Add subcategory', input_html: { class: 'form-control' }

  .form-group
    - @category.children.each do |sub|
      = f.fields_for :children, sub do |child|
        =child.input :name, label: false, input_html: { class: 'form-control' }

  .form-group
    =f.button :submit, class: 'btn btn-primary'
    =link_to "Back", admin_categories_path, class: 'btn btn-default'
